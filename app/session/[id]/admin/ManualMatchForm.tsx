// Superseded by the "จัดคู่เตรียมเอง" form inside UpcomingPlanner.tsx (2026-07-15):
// manual match creation now always goes through คู่เตรียม first (persisted as a
// PendingPair), auto-booking onto a free court only when nobody picked is
// still mid-game. See UpcomingPlanner.tsx and /api/sessions/[id]/pending-pairs.
//
// This file is no longer imported anywhere and is safe to delete — the sandbox
// this change was made in couldn't remove files from the mounted project
// folder, so it's left as a harmless, unused placeholder instead.
export {};
